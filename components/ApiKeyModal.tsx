import React from 'react';

/**
 * Per project guidelines, the API key must be set via environment variables
 * and not through any user interface. This component is intentionally left
 * blank to comply with this requirement and resolve compilation errors.
 */
const ApiKeyModal: React.FC = () => {
  return null;
};

export default ApiKeyModal;
